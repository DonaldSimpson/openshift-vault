apiVersion: v1
kind: BuildConfig
metadata:
  name: vault
  labels:
    build: vault
spec:
  failedBuildsHistoryLimit: 2
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: vault:latest
  runPolicy: Serial
  source:
    type: Binary
  strategy:
    type: Docker
  successfulBuildsHistoryLimit: 2
  triggers: []

